- alias              : bedroom1_dimmer_press_double_off
  id                 : 20cef884-b24d-4eda-be49-360e7487acfd
  trigger:
    - platform       : state
      entity_id      : sensor.hueremote_damiansroom_action
      to             : 'off-press'
  condition:
    - condition      : template
      value_template : '{{ trigger.to_state.attributes.counter == 2 }}'   # Double click only
  action:
    - service        : scene.turn_on
      entity_id      : scene.bedroom_damian_lights_off
